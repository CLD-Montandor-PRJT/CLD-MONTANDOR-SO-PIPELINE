# Customer order PDFs — sensitive, never commit
*/Not_Processed/
*/Processed/

# PdfPig package files — not needed at runtime
lib/*.zip
lib/*.nupkg
lib/pdfpig_pkg/
lib/uglytoad.pdfpig.core_pkg/
lib/uglytoad.pdfpig.fonts_pkg/
lib/uglytoad.pdfpig.tokenization_pkg/
lib/uglytoad.pdfpig.tokens_pkg/

# lib/dlls/ is committed — pipeline loads DLLs from there at runtime

# Internal planning docs
docs/superpowers/

# PDF files anywhere in the tree
*.pdf
*.PDF
